apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "schedule-service.fullname" . }}-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "schedule-service.labels" . | nindent 4 }}
data:
  SPRING_PROFILES_ACTIVE: {{ .Values.env.SPRING_PROFILES_ACTIVE | quote }}
  JAVA_OPTS: {{ .Values.env.JAVA_OPTS | quote }}
  # Management port — exposed separately so Prometheus can scrape without going through the app port
  MANAGEMENT_SERVER_PORT: "8090"
  SERVER_TOMCAT_ACCEPT_COUNT: "200"
  SERVER_TOMCAT_MAX_THREADS: "200"
  # Outbox relay delay (milliseconds) — not a secret, safe to store in ConfigMap
  OUTBOX_RELAY_DELAY_MS: {{ .Values.env.OUTBOX_RELAY_DELAY_MS | quote }}
